---
# tasks file for base-config

- name: Install repository in the machines
  copy: 
    src: 'internal.repo'
    dest: '/etc/yum.repos.d/internal.repo'
    owner: root
    group: root
    mode: '0644'
    
